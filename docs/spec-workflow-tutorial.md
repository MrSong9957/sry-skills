# Claude Code Spec Workflow 零基础教程

> **目标读者**：完全不了解 Spec Workflow 的开发者
> **阅读时间**：15 分钟
> **学习目标**：掌握规范化的 AI 辅助开发流程

---

## 什么是 Spec Workflow？

**Spec Workflow** 是一个自动化 Claude Code 开发流程的工具，它将开发过程标准化为四个阶段：

```
需求分析 → 系统设计 → 任务拆解 → 代码实现
```

### 核心价值

- **规范流程**：避免随意开发，每一步都有文档指导
- **AI 辅助**：Claude Code 根据文档自动生成代码
- **可追溯**：所有决策都记录在文档中
- **可维护**：清晰的文档让后续修改更容易

### 两个工作流

| 工作流 | 适用场景 | 四阶段 |
|--------|---------|-------|
| **Spec Workflow** | 开发新功能 | Requirements → Design → Tasks → Implementation |
| **Bug Fix Workflow** | 修复 Bug | Report → Analyze → Fix → Verify |

---

## 安装与设置

### 前置要求

- **Node.js**: 16+ 版本
- **Claude Code**: 已安装并配置

### 全局安装

```bash
npm install -g @pimzino/claude-code-spec-workflow
```

### 项目初始化

```bash
# 在项目根目录执行
cd your-project
claude-code-spec-workflow
```

**执行后自动创建**：
```
your-project/
├── .claude/
│   ├── commands/       # 14 个斜杠命令
│   ├── steering/      # product.md, tech.md, structure.md
│   ├── templates/     # 文档模板
│   ├── specs/        # 生成的规范文档
│   ├── bugs/         # Bug 修复工作流
│   └── agents/       # AI 助手（默认启用）
```

### 创建项目导向文档（推荐）

```bash
claude
# 在 Claude Code 中输入
/spec-steering-setup
```

这会创建三个核心文档：
- **product.md**: 产品愿景、目标用户、核心功能
- **tech.md**: 技术栈、工具、架构约束
- **structure.md**: 文件组织、命名规范

### 验证安装

```bash
claude-code-spec-workflow --version
# 输出版本号
```

---

## 核心概念

### 1. Spec Workflow 四阶段

```
┌──────────────┐
│ Requirements │ → 明确"要做什么"
└──────────────┘
       ↓
┌──────────────┐
│    Design    │ → 规划"怎么做"
└──────────────┘
       ↓
┌──────────────┐
│    Tasks     │ → 拆解"具体步骤"
└──────────────┘
       ↓
┌──────────────┐
│Implementation│ → 执行"写代码"
└──────────────┘
```

**关键特性**：`/spec-create` 一个命令自动完成前三个阶段！

### 2. Bug Fix Workflow 四阶段

```
┌──────────────┐
│    Report    │ → 描述 Bug 现象
└──────────────┘
       ↓
┌──────────────┐
│   Analyze    │ → 分析根本原因
└──────────────┘
       ↓
┌──────────────┐
│     Fix      │ → 修复代码
└──────────────┘
       ↓
┌──────────────┐
│   Verify     │ → 验证修复效果
└──────────────┘
```

### 3. Steering Documents（导向文档）

三个核心文档为 Claude Code 提供项目上下文：

| 文档 | 作用 | 内容示例 |
|------|------|---------|
| **product.md** | 产品上下文 | 目标用户、核心功能、业务规则 |
| **tech.md** | 技术栈说明 | 框架、库、工具、架构模式 |
| **structure.md** | 项目结构 | 目录组织、文件命名规范 |

**创建命令**：`/spec-steering-setup`

### 4. Agents（AI 助手）

4 个专用 AI 助手，自动执行特定任务：

- **spec-task-executor**: 任务执行器
- **spec-requirements-validator**: 需求验证器
- **spec-design-validator**: 设计验证器
- **spec-task-validator**: 任务验证器

> **注**：Agents 是可选的，不启用也能正常工作

---

## 命令详解

### Spec Workflow 命令

| 命令 | 说明 | 使用场景 |
|------|------|---------|
| `/spec-steering-setup` | 创建项目导向文档 | 新项目初始化 |
| `/spec-create <name> "描述"` | 创建完整规范（需求+设计+任务） | 开始新功能开发 |
| `/spec-execute <task-id> <name>` | 手动执行指定任务 | 实现具体功能 |
| `/<name>-task-<id>` | 自动生成的任务命令 | 快速执行任务 |
| `/spec-status` | 查看规范进度 | 监控开发进度 |
| `/spec-list` | 列出所有规范 | 查看项目所有功能 |

#### `/spec-steering-setup`

**格式**：`/spec-steering-setup`

**作用**：创建项目导向文档（product.md, tech.md, structure.md）

**使用场景**：
- 新项目初始化
- 需要为 Claude Code 提供项目上下文

**示例**：
```bash
claude
/spec-steering-setup

# Claude Code 会：
# 1. 分析你的项目
# 2. 询问产品愿景
# 3. 询问技术栈选择
# 4. 创建 .claude/steering/ 下的三个文档
```

#### `/spec-create`

**格式**：`/spec-create <功能名> "功能描述"`

**作用**：一键生成完整的规范文档（Requirements → Design → Tasks）

**使用场景**：
- 准备开发一个新功能
- 需要系统化地规划开发流程

**示例**：
```bash
/spec-create user-auth "用户认证系统，支持邮箱和手机号登录"

# Claude Code 自动执行：
# 1. 生成 specs/user-auth/requirements.md
# 2. 生成 specs/user-auth/design.md
# 3. 生成 specs/user-auth/tasks.md
# 4. 创建自动生成的任务命令
```

**生成的文档结构**：
```
specs/user-auth/
├── requirements.md  # 需求文档
├── design.md        # 设计文档
└── tasks.md         # 任务清单
```

#### `/spec-execute`

**格式**：`/spec-execute <任务ID> <功能名>`

**作用**：手动执行指定的任务，Claude Code 自动编写代码

**使用场景**：
- tasks.md 已创建
- 准备开始实现具体任务
- 需要手动控制实现顺序

**前提**：必须先执行 `/spec-create`

**示例**：
```bash
# 假设 tasks.md 中有任务 T-1, T-2, T-3

/spec-execute 1 user-auth
# 执行 user-auth 的第 1 个任务

# Claude Code 会：
# 1. 读取 tasks.md 中 T-1 的描述
# 2. 读取 design.md 了解技术方案
# 3. 自动编写代码
# 4. 创建/修改文件
```

#### 自动生成的任务命令

**格式**：`/<功能名>-task-<ID>`

**作用**：快速执行任务，无需手动输入参数

**使用场景**：
- 快速执行已知任务
- 避免重复输入参数

**示例**：
```bash
# 执行 user-auth 的第 1 个任务
/user-auth-task-1

# 等同于
/spec-execute 1 user-auth
```

#### `/spec-status`

**格式**：`/spec-status [功能名]`

**作用**：查看规范开发进度

**使用场景**：
- 监控功能开发进度
- 查看哪些任务已完成

**示例**：
```bash
# 查看所有功能进度
/spec-status

# 查看特定功能进度
/spec-status user-auth
```

#### `/spec-list`

**格式**：`/spec-list`

**作用**：列出项目中所有的规范文档

**使用场景**：
- 查看项目有哪些功能规划
- 快速定位功能文档

**示例**：
```bash
/spec-list

# 输出：
# 📁 项目规范列表
# ✅ user-auth (3/5 tasks completed)
# ⚪ payment-gateway (0/4 tasks)
# 🚫 user-dashboard (blocked)
```

---

### Bug Fix Workflow 命令

| 命令 | 说明 | 使用场景 |
|------|------|---------|
| `/bug-create <name> "描述"` | 创建 Bug 报告 | 记录 Bug 现象 |
| `/bug-analyze` | 分析 Bug 原因 | 找出根本原因 |
| `/bug-fix` | 修复 Bug | 修改代码 |
| `/bug-verify` | 验证修复效果 | 确认问题解决 |
| `/bug-status` | 查看 Bug 状态 | 监控修复进度 |

#### `/bug-create`

**格式**：`/bug-create <Bug名称> "Bug描述"`

**作用**：创建结构化的 Bug 报告文档

**使用场景**：
- 发现 Bug 需要记录
- 需要系统化地跟踪问题

**示例**：
```bash
/bug-create login-timeout "用户登录后 5 分钟就被踢下线，预期是 24 小时"

# Claude Code 会：
# 1. 创建 bugs/login-timeout/bug-report.md
# 2. 引导你填写复现步骤
# 3. 记录预期行为和实际行为
```

**生成的文档结构**：
```
bugs/login-timeout/
└── bug-report.md  # Bug 报告
```

#### `/bug-analyze`

**格式**：`/bug-analyze`

**作用**：分析 Bug 根本原因

**使用场景**：
- Bug 报告已创建
- 需要找出问题根源

**前提**：必须先执行 `/bug-create`

**示例**：
```bash
# 假设已创建 login-timeout bug 报告
/bug-analyze

# Claude Code 会：
# 1. 读取 bug-report.md
# 2. 检查相关代码
# 3. 分析配置文件
# 4. 生成 bugs/login-timeout/bug-analyze.md
```

**生成的分析文档**：
```markdown
## 根本原因
JWT 配置错误

## 问题代码
`config/jwt.js` 第 5 行：
expiresIn: '5m'  // 错误：应该是 '24h'

## 影响范围
- 所有登录用户
- 生产环境

## 修复方案
修改 `config/jwt.js`:
expiresIn: '24h'
```

#### `/bug-fix`

**格式**：`/bug-fix`

**作用**：根据分析报告修复 Bug

**使用场景**：
- Bug 原因已明确
- 需要修改代码

**前提**：必须先执行 `/bug-analyze`

**示例**：
```bash
# 假设已完成 bug-analyze
/bug-fix

# Claude Code 会：
# 1. 读取 bug-analyze.md
# 2. 修改 config/jwt.js
# 3. 将 '5m' 改为 '24h'
# 4. 生成 bugs/login-timeout/bug-fix.md
```

#### `/bug-verify`

**格式**：`/bug-verify`

**作用**：验证 Bug 是否已修复

**使用场景**：
- Bug 修复代码已提交
- 需要确认问题已解决

**示例**：
```bash
/bug-verify

# Claude Code 会：
# 1. 运行相关测试
# 2. 手动测试复现场景
# 3. 生成 bugs/login-timeout/bug-verify.md

# 验证结果：
# ✅ Token 有效期正确（24h）
# ✅ 所有测试通过
```

#### `/bug-status`

**格式**：`/bug-status`

**作用**：查看 Bug 修复进度

**使用场景**：
- 监控 Bug 修复状态
- 查看有哪些待修复的 Bug

**示例**：
```bash
/bug-status

# 输出：
# 🐛 Bug 修复进度
# ✅ login-timeout (已修复)
# ⚠️ payment-crash (分析中)
# ⚪ api-timeout (未开始)
```

---

## 实战示例

### 示例 1：创建用户认证功能

**场景**：需要实现一个完整的用户认证系统

#### 步骤 1：创建项目导向文档（推荐）

```bash
claude
/spec-steering-setup
```

**回答 Claude Code 的问题**：
- 产品愿景是什么？
- 目标用户是谁？
- 使用什么技术栈？

**生成 `.claude/steering/` 三个文档**：
```markdown
# product.md
## 产品愿景
构建现代化的 Web 应用平台

## 目标用户
- 企业开发者
- 技术团队

## 核心功能
- 用户认证
- 数据管理
- API 接口
```

#### 步骤 2：创建功能规范

```bash
/spec-create user-auth "用户认证系统，包括注册、登录、登出功能"
```

**Claude Code 自动生成三个文档**：

**`specs/user-auth/requirements.md`**：
```markdown
## 功能概述
实现完整的用户认证系统

## 功能列表
- [ ] 用户注册（邮箱/密码）
- [ ] 用户登录（邮箱/密码）
- [ ] JWT Token 认证
- [ ] 登出功能

## 验收标准
- 注册成功发送欢迎邮件
- 登录成功返回 JWT Token
- Token 有效期 24 小时
- 密码加密存储（bcrypt）
```

**`specs/user-auth/design.md`**：
```markdown
## 技术栈
- 后端：Express.js
- 数据库：PostgreSQL
- 认证：Passport.js + JWT
- 加密：bcrypt

## API 设计
POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout

## 数据模型
Users 表：
- id: UUID PRIMARY KEY
- email: VARCHAR UNIQUE
- password_hash: VARCHAR
- created_at: TIMESTAMP
```

**`specs/user-auth/tasks.md`**：
```markdown
## 任务清单

### T-1: 数据库设置
- [ ] 创建 Users 表
- [ ] 添加唯一索引

### T-2: 注册功能
- [ ] POST /api/auth/register
- [ ] 密码加密
- [ ] 输入验证

### T-3: 登录功能
- [ ] POST /api/auth/login
- [ ] JWT 签发
- [ ] 错误处理

### T-4: 测试
- [ ] 单元测试
- [ ] 集成测试
```

#### 步骤 3：执行任务

```bash
# 方法 1：使用 spec-execute
/spec-execute 1 user-auth

# 方法 2：使用自动生成的命令
/user-auth-task-1
```

**Claude Code 自动创建**：
```
migrations/001_create_users.sql
src/models/User.js
```

```bash
/user-auth-task-2
```

**Claude Code 自动创建**：
```
src/routes/auth.js
src/controllers/authController.js
src/middleware/validation.js
```

```bash
/user-auth-task-3
```

**Claude Code 自动创建**：
```
src/services/jwt.js
src/middleware/auth.js
```

```bash
/user-auth-task-4
```

**Claude Code 自动创建**：
```
tests/auth.test.js
tests/integration/auth.test.js
```

#### 步骤 4：查看进度

```bash
/spec-status user-auth

# 输出：
# ✅ user-auth 进度
# ✅ T-1: 数据库设置 (已完成)
# ✅ T-2: 注册功能 (已完成)
# ✅ T-3: 登录功能 (已完成)
# ⚪ T-4: 测试 (进行中)
#
# 总体进度: 3/4 任务完成 (75%)
```

---

### 示例 2：修复登录超时 Bug

**场景**：用户反馈登录后很快就被踢下线

#### 步骤 1：创建 Bug 报告

```bash
/bug-create login-timeout "用户登录后 5 分钟就被踢下线，预期是 24 小时"
```

**生成 `bugs/login-timeout/bug-report.md`**：
```markdown
## Bug 描述
登录后 Token 过期太快

## 复现步骤
1. 使用邮箱/密码登录
2. 等待 5 分钟
3. 任何请求返回 401 错误

## 预期行为
Token 应该 24 小时有效

## 实际行为
Token 5 分钟后失效

## 环境
- 生产环境
- Node.js 18.x
```

#### 步骤 2：分析 Bug 原因

```bash
/bug-analyze
```

**生成 `bugs/login-timeout/bug-analyze.md`**：
```markdown
## 根本原因
JWT 配置错误

## 问题代码
`config/jwt.js` 第 5 行：
```javascript
expiresIn: '5m'  // 错误：应该是 '24h'
```

## 影响范围
- 所有登录用户
- 生产环境

## 修复方案
修改 `config/jwt.js`:
```javascript
expiresIn: '24h'
```
```

#### 步骤 3：修复 Bug

```bash
/bug-fix
```

**Claude Code 自动修改代码**：
```javascript
// config/jwt.js
module.exports = {
  jwt: {
    secret: process.env.JWT_SECRET,
-   expiresIn: '5m'
+   expiresIn: '24h'
  }
}
```

**生成 `bugs/login-timeout/bug-fix.md`**：
```markdown
## 修复内容
修改 JWT 配置，将过期时间从 5 分钟改为 24 小时

## 修改文件
- config/jwt.js (第 5 行)

## 测试建议
1. 运行单元测试
2. 手动测试登录流程
3. 验证 Token 有效期
```

#### 步骤 4：验证修复

```bash
/bug-verify
```

**生成 `bugs/login-timeout/bug-verify.md`**：
```markdown
## 验证结果

### 测试执行
✅ 单元测试通过
✅ 集成测试通过
✅ Token 有效期测试通过

### 手动验证
✅ 登录后 24 小时内 Token 有效
✅ 24 小时后正确返回 401

### 结论
Bug 已修复，可以部署
```

#### 步骤 5：查看 Bug 状态

```bash
/bug-status

# 输出：
# 🐛 Bug 修复进度
# ✅ login-timeout (已修复)
# ⚠️ payment-crash (分析中)
# ⚪ api-timeout (未开始)
```

---

## 高级功能

### 1. 实时监控 Dashboard

```bash
npx -p @pimzino/claude-code-spec-workflow claude-spec-dashboard
```

**功能**：
- 📊 实时进度追踪
- 🔄 WebSocket 自动更新
- 📝 Git 集成
- 🎨 现代化 UI（Tailwind CSS）

**Dashboard Tunnel（外部分享）**：
```bash
# 启动带 tunnel 的 dashboard
npx -p @pimzino/claude-code-spec-workflow claude-spec-dashboard --tunnel

# 带密码保护
npx -p @pimzino/claude-code-spec-workflow claude-spec-dashboard --tunnel --tunnel-password mySecret123

# 选择特定 provider
npx -p @pimzino/claude-code-spec-workflow claude-spec-dashboard --tunnel --tunnel-provider cloudflare
```

### 2. 上下文优化

优化 Claude Code 的上下文使用，减少 60-80% token 消耗：

```bash
# 加载所有导向文档
claude-code-spec-workflow get-steering-context

# 加载所有规范文档
claude-code-spec-workflow get-spec-context user-auth

# 加载所有模板
claude-code-spec-workflow get-template-context
```

### 3. CLI 选项

```bash
# 基础设置
claude-code-spec-workflow

# 高级选项
claude-code-spec-workflow --project /path/to/project --force --yes

# 测试设置
claude-code-spec-workflow test
```

### 4. 最佳实践

#### ✅ 推荐做法

1. **先创建导向文档**
   ```bash
   /spec-steering-setup  # 为 Claude Code 提供项目上下文
   ```

2. **使用 spec-create 一键生成**
   ```bash
   /spec-create feature-name "描述"  # 自动生成需求+设计+任务
   ```

3. **选择合适的执行方式**
   ```bash
   # 小功能：自动命令
   /feature-name-task-1

   # 复杂功能：手动控制
   /spec-execute 1 feature-name
   ```

4. **定期查看进度**
   ```bash
   /spec-status  # 功能进度
   /bug-status   # Bug 进度
   ```

#### ❌ 避免做法

1. **跳过导向文档**
   - 没有 steering 文档
   - Claude Code 不了解项目上下文
   - 生成代码不符合项目规范

2. **手动拆分阶段**
   - 不需要手动创建 requirements.md
   - `/spec-create` 会自动生成所有文档
   - 避免重复工作

3. **忽略进度追踪**
   - 不使用 `/spec-status`
   - 不知道哪些任务已完成
   - 容易遗漏任务

---

## 快速参考

### Spec Workflow 流程图

```
┌─────────────────────┐
│ /spec-steering-setup│ 创建项目上下文
└─────────┬───────────┘
          ↓
┌─────────────────────┐
│ /spec-create name   │ 一键生成规范
└─────────┬───────────┘
          ↓
    ┌─────┴─────┐
    │           │
┌───▼────┐  ┌──▼─────────┐
│自动命令 │  │手动控制     │
│/name-  │  │/spec-      │
│task-1  │  │execute 1   │
└────────┘  └────────────┘
     ↓            ↓
┌─────────────────────┐
│ /spec-status        │ 查看进度
└─────────────────────┘
```

### Bug Fix Workflow 流程图

```
┌──────────────────┐
│ /bug-create name │ 创建 Bug 报告
└────────┬─────────┘
         ↓
┌──────────────────┐
│ /bug-analyze     │ 分析根本原因
└────────┬─────────┘
         ↓
┌──────────────────┐
│ /bug-fix         │ 修复代码
└────────┬─────────┘
         ↓
┌──────────────────┐
│ /bug-verify      │ 验证修复效果
└────────┬─────────┘
         ↓
┌──────────────────┐
│ /bug-status      │ 查看修复进度
└──────────────────┘
```

---

## 常见问题

### Q1：必须先创建 steering 文档吗？

**A**：推荐但不强制：

```bash
# ✅ 推荐（有项目上下文）
/spec-steering-setup → /spec-create feature

# ⚠️ 可接受（无项目上下文）
/spec-create feature  # Claude Code 会通用处理
```

### Q2：`/spec-create` 和 `/spec-execute` 有什么区别？

**A**：
- `/spec-create`: 生成文档（requirements + design + tasks）
- `/spec-execute`: 执行任务（编写代码）

```bash
# 第 1 步：生成文档
/spec-create user-auth "描述"

# 第 2 步：执行任务
/spec-execute 1 user-auth
```

### Q3：可以跳过某些阶段吗？

**A**：不推荐，`/spec-create` 会自动完成所有阶段：

```
❌ 错误做法：
手动创建 requirements.md
手动创建 design.md
手动创建 tasks.md

✅ 正确做法：
/spec-create feature-name "描述"  # 一键生成所有文档
```

### Q4：自动生成的任务命令是什么？

**A**：`/spec-create` 执行后自动创建：

```bash
# 假设 tasks.md 有 5 个任务
/feature-name-task-1  # 执行第 1 个任务
/feature-name-task-2  # 执行第 2 个任务
/feature-name-task-3  # 执行第 3 个任务
/feature-name-task-4  # 执行第 4 个任务
/feature-name-task-5  # 执行第 5 个任务

# 等同于
/spec-execute 1 feature-name
/spec-execute 2 feature-name
/spec-execute 3 feature-name
/spec-execute 4 feature-name
/spec-execute 5 feature-name
```

### Q5：如何与 Git 集成？

**A**：每个阶段提交一次：

```bash
# 完成规范创建
git add specs/feature-name/
git commit -m "docs: add user-auth spec"

# 完成实现
git add src/
git commit -m "feat: implement user-auth"
```

### Q6：Bug 修复流程必须按顺序执行吗？

**A**：推荐顺序执行：

```bash
# ✅ 推荐（完整流程）
/bug-create → /bug-analyze → /bug-fix → /bug-verify

# ⚠️ 可接受（跳过验证）
/bug-create → /bug-analyze → /bug-fix

# ❌ 不推荐（跳过分析）
/bug-create → /bug-fix  # 缺少原因分析
```

---

## 下一步

现在你已经掌握了 Spec Workflow 的基础：

1. **实践**：尝试开发一个小功能
2. **进阶**：学习使用 Dashboard 监控
3. **定制**：根据项目调整模板

### 推荐学习路径

```
第1周：使用 /spec-create 开发小功能
       ↓
第2周：尝试 Bug Fix Workflow
       ↓
第3周：配置 Dashboard 实时监控
       ↓
第4周：定制项目模板
```

---

## 附录：命令速查表

### Spec Workflow

| 命令 | 快捷描述 |
|------|---------|
| `/spec-steering-setup` | 创建项目导向文档 |
| `/spec-create <name> "描述"` | 一键生成完整规范 |
| `/spec-execute <id> <name>` | 手动执行任务 |
| `/<name>-task-<id>` | 自动生成的任务命令 |
| `/spec-status` | 查看规范进度 |
| `/spec-list` | 列出所有规范 |

### Bug Fix Workflow

| 命令 | 快捷描述 |
|------|---------|
| `/bug-create <name> "描述"` | 创建 Bug 报告 |
| `/bug-analyze` | 分析 Bug 原因 |
| `/bug-fix` | 修复 Bug |
| `/bug-verify` | 验证修复效果 |
| `/bug-status` | 查看 Bug 状态 |

### 工具命令

| 命令 | 作用 |
|------|------|
| `claude-code-spec-workflow` | 初始化项目 |
| `claude-spec-dashboard` | 启动监控面板 |
| `get-steering-context` | 加载导向文档 |
| `get-spec-context <name>` | 加载规范文档 |
| `get-template-context` | 加载模板文档 |

---

## 推荐使用策略

| 场景 | 推荐命令 |
|------|---------|
| 新功能，定义明确 | `/spec-execute` 或自动任务命令 |
| 复杂/实验性功能 | `/spec-execute`（手动控制） |
| 已有代码的 Bug | Bug workflow（`/bug-create` → `/bug-verify`） |
| 学习代码库 | 手动执行各个命令 |
| 生产部署 | 完整 spec workflow + 完成审查 |

---

**祝你使用 Spec Workflow 开发愉快！** 🚀

有问题？查看 [GitHub 仓库](https://github.com/Pimzino/claude-code-spec-workflow) 或查阅 [Claude Code 官方文档](https://docs.anthropic.com/claude-code)
